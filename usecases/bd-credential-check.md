# Bright Data 자격증명 확인

확인 시각 2026-08-04T12:51:06+09:00

토큰 값은 기록하지 않는다. 환경변수 존재 여부와 API 응답만 남긴다.

| 환경변수 | 설정 여부 | 길이 |
|---|---|---:|
| `BRIGHTDATA_API_KEY` | 없음 | 0 |
| `BRIGHTDATA_API_TOKEN` | 설정됨 | 36 |

## API 응답

| 확인 항목 | HTTP | 응답 |
|---|---|---|
| 계정 상태 | 401 | `Token expired` |
| 스크래퍼 목록 | 401 | `Token expired` |

판정: **인증 실패 (만료 또는 무효)**

## 이 결과가 뜻하는 것

판정이 사용 가능이 아니면 각 파이프라인의 Bright Data 경로는 `--mock`으로만 돈다.
그 경로에서 나온 행은 `data_origin` 컬럼에 mock으로 표시되며 실제 수집 결과가 아니다.
공개 소스에서 나온 행은 같은 컬럼에 real로 표시된다.
