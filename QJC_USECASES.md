# QJC가 실제로 쓰는 웹데이터 시스템 3개

2026년 8월 4일에 만들고 그날 바로 돌린 기록이다. 기능 시연이 아니라, QJC가 실제로 겪던
문제 세 개를 골라 시스템으로 만들고 진짜 데이터로 결과를 뽑았다.

Bright Data API 키는 아직 없다. 그래서 두 가지를 동시에 했다. 지금 접근할 수 있는 공개
데이터로 실제 결과를 만들었고, 키가 오면 그대로 범위가 넓어지도록 Bright Data 경로를
코드에 붙여 픽스처로 검증했다. 어느 행이 진짜이고 어느 행이 픽스처인지는 모든 CSV의
`data_origin` 컬럼에 남겼다.

키 상태는 주장이 아니라 기록으로 남겼다. `usecases/bd-credential-check.md`에 확인 결과가 있다.
환경변수 `BRIGHTDATA_API_TOKEN`은 설정되어 있지만 API가 `401 Token expired`를 돌려준다.

## 한눈에 보기

| 사례 | 무엇을 풀었나 | 실제 산출물 | 실데이터 / mock |
|---|---|---|---|
| 리드 발굴 (메인) | 손으로 하던 고객 발굴 | 점수 매겨진 리드 210건, 컬럼 30개 | 실 207 / mock 3 |
| 경쟁사 모니터링 | 사이트를 하나씩 열어 보던 가격 조사 | 경쟁 프로그램 59건, 브랜드 5개 비교표 | 실 57 / mock 2 |
| 콘텐츠 소재 발굴 | 감으로 정하던 다음 영상 주제 | 영상 342건, 주제 후보 29개 랭킹 | 실 342 / mock 0 |

세 시스템 모두 표준 라이브러리만 쓴다. `pip install` 없이 그대로 돈다.

---

# 사례 1. 리드 발굴 (메인)

## 문제

QJC의 오늘 할 일 1순위가 "아웃바운드 영업 실행 계획"이다. AI 자동화를 파는 회사가 정작
자기 고객 발굴은 손으로 하고 있었다.

누구에게 연락할지 정하려면 채용 사이트와 회사 홈페이지를 사람이 하나씩 열어 보고
판단해야 한다. 한 곳을 판단하는 데 10분에서 20분이 걸린다. 그렇게 찾아도 "이 회사가 왜
우리 고객인가"는 찾은 사람 머릿속에만 남는다. 다른 사람에게 넘길 수 없고, 다음 주에
다시 하면 처음부터다.

## 가설

기존 고객 62건을 업종과 서비스 유형으로 집계했다. 고객 실명과 거래액은 쓰지 않고
집계만 봤다. 한 가지가 보였다. QJC 고객의 다수는 AI가 본업이 아닌 회사다. AI를 도입하고
싶은데 내부에 사람이 없어서 교육을 사거나 구축을 맡긴다.

> AI가 본업이 아닌 회사가 AI 또는 데이터 직무를 채용하기 시작했다면,
> AI 전환 의지와 예산은 생겼지만 내부 역량은 아직 얇다는 뜻이다.

여기에 더 날카로운 신호를 하나 찾았다. 공고 자격 요건에 Claude Code나 Cursor 같은
AI 코딩 도구 이름이 직접 적혀 있는 경우다. 도구는 이미 정했는데 그걸 쓸 사람을 못 구한
상태다. QJC가 파는 것이 정확히 그 사람을 만드는 일이다.

## 해결

```
build_icp.py   기존 고객 62건 집계 -> ICP 정의와 채점 규칙
collect.py     공개 채용 API 수집 -> 정규화된 공고
enrich.py      회사 단위 집계 + 공공데이터 보강
score.py       ICP 점수 -> 우선순위가 붙은 리드 CSV와 접근 각도 메모
```

데이터 출처는 다섯 곳이다. 접근 판정은 코드가 실행 시점에 robots.txt를 직접 읽어서 한다.

| 소스 | 얻은 것 | 판정 |
|---|---|---|
| 점핏 공개 포지션 API | 공고 703건 | robots.txt에서 `/positions` 허용 확인 |
| 점핏 공고 상세 API | 회사 소개, 자격 요건, 요구 도구 | 같음 |
| 원티드 공개 채용 API | 공고 900건, 사업자등록번호 | robots.txt를 CDN이 403으로 막아 확인 불가, 최소 요청만 |
| 국세청 사업자등록상태 | 계속사업자 여부, 과세유형 | 공식 오픈 API |
| DART 공시대상 법인 명부 | 법인 11만 707건 | 공식 오픈 API |

채점은 정신호와 역신호를 함께 계산한다. 비테크 업종의 AI 채용에 +24, AI 코딩 도구 요구에
+14를 주고, AI 자체가 본업인 회사에는 -30을 준다. 우리 서비스가 필요 없는 회사이기 때문이다.

담당 분기도 자동이다. 규모 기준선을 넘으면 시니어 담당, 아래면 일반 담당으로 붙인다.
기준선과 담당자 이름은 `score.py` 상단 상수라 조직에 맞게 바꿔 쓰면 된다.

## 결과

| 항목 | 수치 |
|---|---:|
| 수집한 공고 | 1,607건 (실데이터 1,603 + 픽스처 4) |
| 집계된 기업 | 754곳 |
| AI 또는 자동화 신호 보유 기업 | 210곳 |
| 채점된 리드 | 210건 (실 207 / mock 3) |
| A티어, 즉시 접촉 | 17건 |
| B티어, 2주 내 | 94건 |
| C티어, 모니터링 | 99건 |
| 국세청으로 사업자 상태 확인 | 116곳 |
| DART 공시대상 법인 대조 | 91곳 |
| 공고에 AI 코딩 도구를 명시한 기업 | 10곳 |
| 대표 담당 / PM 담당 자동 분류 | 69건 / 119건 |

가장 위에 올라온 리드는 임직원 160명 규모의 의약품 주문 플랫폼이었다. 데이터 엔지니어와
데이터 사이언티스트를 동시에 뽑고 있고, 자격 요건에 이렇게 적어 두었다.

> "AI 코딩도구(Claude Code, Cursor 등)를 실무에 활용하며,
> 그 결과를 검증하는 자기기준을 갖고 있는 분"

QJC가 파는 것과 정확히 겹친다. 이 회사를 사람이 손으로 찾으려면 채용 공고 1,603건을
읽어야 했다.

산출 파일은 `usecases/lead-discovery/results/`에 있다.

- `leads_20260804.csv` 리드 210건, 컬럼 30개. 점수와 근거가 같은 행에 있다
- `top-leads.md` 상위 15건의 접근 각도 메모
- `scoring-summary.md` 티어와 업종 분포, 규모 추정의 한계
- `icp-evidence.md` 기존 고객 62건에서 ICP를 뽑은 근거 집계
- `run-log.txt` 실행 로그 전문

## 이 사례가 보여 주는 한계

같은 파이프라인 안에서 못 채운 칸이 그대로 남아 있다. 이게 Bright Data가 필요한 지점이다.

- 임직원 수를 확보한 곳은 210곳 중 1곳뿐이다. 회사 소개문에 우연히 적혀 있던 경우다.
  공개 채용 API에는 회사 규모가 구조화된 필드로 없다.
- 210곳 중 58곳은 업종이 미분류로 남았다. 회사 소개문이 아예 없거나 근거가 약해서다.
- 회사 규모·업종·임직원 증감 같은 기업 정보는 개별 기업의 공개 페이지에 흩어져 있어, 공개 채용 API만으로는 채울 수 없었다.

---

# 사례 2. 경쟁사 모니터링

## 문제

QJC는 AI 코딩 부트캠프와 VOD, 기업 강의를 판다. 가격을 정하거나 새 기수를 기획할 때마다
경쟁사 커리큘럼과 가격, 모집 현황을 사람이 사이트를 하나씩 열어 보며 확인한다. 매번 같은
일을 반복하고, 확인한 결과는 문서로 남지 않는다.

## 해결

경쟁 브랜드의 강의명, 판매가, 정가, 기간, 형태, 모집상태를 수집해 비교표를 만든다.
대상마다 robots.txt를 실행 시점에 읽어 판정하고, 금지된 곳은 건너뛰고 사유를 기록한다.

## 결과

국내 AI·개발 교육 플랫폼을 대상으로 한 내부 실행 기록이다. 어느 회사인지는 적지 않는다.
리포에 들어 있는 `targets.json`은 `example.com` 자리표시자라 이 숫자가 그대로 재현되지는 않는다.

| 항목 | 수치 |
|---|---:|
| 수집한 프로그램 | 59건 (실데이터 57 + 픽스처 2) |
| 브랜드 | 4곳 (19 / 14 / 12 / 12건) + 픽스처 1곳 2건 |
| 가격을 실제로 읽어낸 행 | 55건 |
| robots.txt로 차단 확인된 소스 | 2곳 (`/catalog`, `/products/*` Disallow) |

읽어낸 것

- 유료 43건의 가격은 1만원에서 220만원 사이, 중앙값 24만 3천원이다.
- 정가와 판매가가 함께 노출된 20건의 할인율 중앙값이 45%다. 정가를 걸어 두고 상시
  할인가로 파는 방식이 이 표본의 기본값이다. QJC가 정가 하나만 노출하면 같은 화면에서
  더 비싸 보인다.
- 프로그램명에 가장 자주 나온 키워드는 AI 41건, 코딩 14건, 코드 8건, 바이브 8건이다.
  Claude는 6건 나왔는데 QJC 상품명에는 드러나 있지 않다.

산출 파일은 `usecases/competitor-watch/results/`에 있다. `comparison.md`에 비교표와
포지셔닝 시사점이, `blocked-sources.md`에 접근하지 못한 소스와 사유가 있다.

## 이 사례가 보여 주는 한계

- 한 곳은 robots.txt가 허용하는데도 0건이 나왔다. 목록과 상세가 모두
  자바스크립트로 렌더링되어 정적 수집으로는 아무것도 잡히지 않는다. Bright Data가
  필요해지는 자리가 여기다.
- 두 곳은 robots.txt가 강의 목록 경로를 금지한다. 접근하지 않았다.
- 표본은 접근 가능했던 4곳 57건이다. 시장 전체 분포가 아니다.

---

# 사례 3. 콘텐츠 소재 발굴

## 문제

QJC 유튜브 채널은 구독자 3만 3천 명이다. 그런데 다음 영상 주제를 감으로 정한다. 어떤
키워드가 실제로 조회수를 내는지, 경쟁 채널이 무엇으로 터졌는지 데이터가 없다. 주제 하나를
잘못 고르면 제작에 들어간 며칠이 그대로 날아간다.

## 해결

두 축을 함께 본다. 수요축은 사람들이 실제로 검색하는 키워드의 상위 영상 조회수 분포이고,
공급축은 경쟁 채널에서 실제로 터진 영상의 역설계다. 여기에 자사 채널 기준선을 겹쳐서
"우리 평균보다 큰 주제인가"를 판단한다.

점수는 조회수, 하루 조회수, 구독자 대비 배수, 90일 이내 비중, 경쟁 밀도를 가중 합산한다.
각 점수의 원천 수치를 CSV 컬럼으로 함께 남긴다. 점수만 있고 근거가 없으면 쓸 수 없다.

## 결과

| 항목 | 수치 |
|---|---:|
| 검색 키워드 | 9개 |
| 수집한 영상 | 342건 (전부 실데이터) |
| 검색 결과에서 나온 고유 영상 | 244건 |
| 경쟁 채널 업로드 | 72건 |
| 주제 후보 | 29개, 상위 10개 상세 분석 |
| 자사 채널 기준선 | 최근 180일 상위 25개 영상 조회수 중앙값 32,109회 |

읽어낸 것

- 1순위 후보의 조회수 중앙값이 5만 466회로 자사 기준선의 1.57배다.
- 그 주제에서 구독자 2만 7천 명 채널이 17만 회를 냈다. 구독자 대비 6.21배다. 채널 규모가
  아니라 주제가 끌어온 조회수라는 뜻이다.
- 그 주제를 잡은 10만 구독자 이상 채널이 아직 0개다.
- 상위 영상 제목은 5개 중 4개에 숫자가 들어 있고, 영상 길이 중앙값이 24분이다.

산출 파일은 `usecases/content-radar/results/`에 있다. `topic-shortlist.md`에 상위 10개
주제와 근거 영상, 제목 패턴, 앵글 제안이 있다.

## 이 사례가 보여 주는 한계

- 공개 지표만 본다. 시청 지속률과 클릭률은 자사 채널 것만 볼 수 있고 경쟁 채널 것은 없다.
- 한 번에 키워드 9개, 영상 342건이 한계다. 순차 수집이라 여기서 더 늘리면 실행 시간이
  선형으로 늘어난다.

---

# 실제 데이터와 mock 구분

주장이 아니라 파일로 확인할 수 있다. 모든 CSV에 `data_origin` 컬럼이 있다.

| 파이프라인 | 실데이터 | mock | mock의 출처 |
|---|---:|---:|---|
| 리드 발굴 | 207행 | 3행 | Bright Data 공개 채용공고 픽스처 (`mock/`) |
| 경쟁사 모니터링 | 57행 | 2행 | JS 렌더 사이트 자리 표시 픽스처 |
| 콘텐츠 소재 발굴 | 342행 | 0행 | 별도 폴더 `results/bd-mock-check/`에서만 검증 |

mock 행은 전부 Bright Data 경로가 코드로 연결되어 있는지 확인하려고 만든 합성 레코드다.
회사명과 수치가 전부 가상이다. 실제 성과로 인용하면 안 된다.

실데이터의 출처는 다음과 같다. 전부 공개 API이거나 robots.txt가 허용한 경로다.

- 점핏 공개 포지션 API, 원티드 공개 채용 API
- 국세청 사업자등록상태 API, DART 공시대상 법인 명부
- 국내 AI·개발 교육 플랫폼 4곳의 공개 강의 페이지 (robots.txt 허용 경로만)
- yt-dlp로 수집한 유튜브 공개 지표, QJC 자사 채널 통계

# Bright Data 키가 오면 달라지는 것

세 시스템 모두 `bd_client.py`를 그대로 쓴다. `--mock`을 떼면 같은 코드가 실제 API를 부른다.
바뀌는 것은 세 가지다.

**막힌 곳이 열린다.** 지금 못 간 곳이 명확하다. 공개 기업 페이지는 CAPTCHA 같은 안티봇
조치와 사이트마다 다른 페이지 구조 때문에 수작업으로는 규모 있게 못 갔고, 원티드는
robots.txt 확인 불가, 교육 플랫폼 한 곳은 자바스크립트 렌더링이 벽이다. 다른 두 곳은
robots.txt 금지다. 앞의 셋은 Bright Data가 프록시 로테이션·안티봇 처리를 자동화해 지원하는
공개 데이터 영역이고, 뒤의 둘은 키가 있어도 접근하지 않는다. 사이트가 명시적으로 금지한
경로이기 때문이다. 로그인이 필요한 폐쇄 영역도 같은 이유로 수집 대상이 아니다.

**빈 칸이 채워진다.** 리드 210곳 중 임직원 수를 확보한 곳은 1곳, 업종 미분류가 58곳이다.
공개 기업 페이지의 회사 정보를 Scraper API로 구조화해 수집하면, 지금 채점에서 비테크
가점을 못 받고 있는 58곳이 제자리를 찾는다.

**규모가 커진다.** 지금은 한국 공개 API 두 곳에서 공고 1,603건, 유튜브 342건, 강의 57건을
봤다. 순차 수집이라 여기서 더 늘리면 실행 시간이 그만큼 늘어난다. 병렬 수집으로 넘어가면
해외 법인, 영문 공고, 다른 플랫폼 교차 신호까지 같은 채점 규칙에 넣을 수 있다.

---

# 실행 방법

```bash
cd usecases/lead-discovery     && ./run.sh
cd usecases/competitor-watch   && ./run.sh
cd usecases/content-radar      && ./run.sh
```

Bright Data 키를 받은 뒤에는 각 스크립트에 `--live`를 붙인다.

필요한 환경변수는 `.env.example`에 이름만 적어 두었다. 값은 저장소에 넣지 않는다.

| 변수 | 쓰는 곳 | 없으면 |
|---|---|---|
| `DATA_GO_NTS_API_KEY` | 국세청 사업자등록상태 | 해당 보강만 건너뛴다 |
| `DART_API_KEY` | DART 공시대상 법인 명부 | 규모 추정만 건너뛴다 |
| `BRIGHTDATA_API_KEY` | Bright Data Web Scraper API | 픽스처로만 동작한다 |

# 수집 예절

세 시스템 모두 같은 규칙을 지킨다.

- robots.txt를 실행 시점에 직접 읽고 판정한다. 캐시한 판정을 재사용하지 않는다.
- 금지된 경로는 접근하지 않고 사유를 결과 파일에 남긴다.
- 호스트별로 요청 간 최소 간격을 둔다.
- User-Agent에 `QJC-research/1.0 (+https://qjc.app)`로 누가 보내는 요청인지 밝힌다.
- 로그인이 필요한 영역에는 들어가지 않는다.
